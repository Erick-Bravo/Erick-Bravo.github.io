import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const AppSyled = styled(Box)({
   display: "flex",
   justifyContent: "center",
   height: "100vh",
   width: "100vw",
   padding: 0,
   margin: 0,
   backgroundImage: "linear-gradient(#4D3C83, #D56172)",
});
