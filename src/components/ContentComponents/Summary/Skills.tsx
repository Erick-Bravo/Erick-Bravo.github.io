import styled from '@emotion/styled';
import { Box } from '@mui/material';

const SkillsContainer = styled(Box)({
     marginTop: "20px",
     fontWeight: "bold",
})

const Skills = () => {
    return (
        <SkillsContainer>
             React, JavaScript, Typescript, HTML, CSS, GSAP Animations, Redux Toolkit, Material UI, Chakra UI, Emotion Styling, GraphQL, 
             Node.js, Next.js, Express, MongoDB, NoSQL, SQL, Git, 
             GitHub, Jira, Agile, Scrum, Kanban, Figma, Vercel, Heroku, Railway.
             
              
        </SkillsContainer>
    );
};

export default Skills;